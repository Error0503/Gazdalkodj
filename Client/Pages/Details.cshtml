@page
@model Client.Pages.DetailsModel
@{
}

<!DOCTYPE html>
<html>
<head>
    <title>Részletek</title>
</head>
<body>
    <div class="text-center">
        @{ 
            if (Model.Status == 1)
            {
                <p>@Model.Output</p>
            } else
            {
                <p style="color:red">@Model.Output</p>
            }
        }
    </div>
</body>
</html>